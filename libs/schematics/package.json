{
  "name": "@realworld/schematics",
  "version": "0.0.1",
  "schematics": "./collection.json",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && copyfiles -s false -u 1 ./**/templates/**/* ./dist",
    "watch": "tsc -p tsconfig.json --watch",
    "test": "npm run build && jasmine src/**/*_spec.js"
  },
  "devDependencies": {
    "@types/jasmine": "^4.3.1",
    "@types/pluralize": "^0.0.29",
    "copyfiles": "^2.4.1",
    "jasmine": "^4.5.0"
  }
}
